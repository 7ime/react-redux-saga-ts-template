import {PATHS_ROOT_ROUTES} from '../../../navigation/routes';

const root = PATHS_ROOT_ROUTES.formScene;

export const PATH_FORM_SCENE_ROUTES = {
    firstExample: root + '/example-1',
    secondExample: root + '/example-2',
};